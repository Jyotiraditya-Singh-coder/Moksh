# Moksh Frontend

Project scaffold created to match the shared folder structure.

## Run locally

1. Install dependencies:
   npm install
2. Start dev server:
   npm run dev

Then open http://localhost:3000
