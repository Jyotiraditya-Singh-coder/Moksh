const fs = require('fs'); const file = 'c:/Users/prave/OneDrive/Desktop/Moksh/frontend/public/dashboard/Dashboard.html'; const lines = fs.readFileSync(file, 'utf8').split('\n'); if (!lines.some(l => l.includes('brain-map.js'))) { const bodyEnd = lines.findIndex(l => l.includes('</body>')); if (bodyEnd !== -1) { lines.splice(bodyEnd, 0, '    <script src=\"brain-map.js\" defer></script>'); fs.writeFileSync(file, lines.join('\n')); console.log('Successfully added to public dashboard'); } }
